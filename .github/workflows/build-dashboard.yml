name: Actualizar Sistema de Evidencias OPS

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]
    paths:
      - 'cms/**.xlsx'
      - 'assets/dm/**'
      - 'assets/ui/**'
      - 'config/settings.json'
      - 'scripts/build_dashboard.py'
      - 'scripts/export_excel.py'
      - 'scripts/export_pdf.py'
      - 'scripts/audit_project.py'
      - 'tests/**'
      - 'index.html'
      - 'styles.css'
      - 'app.js'
      - 'pdf-export.js'
      - 'xlsx-export.js'
      - 'service-worker.js'
      - 'manifest.webmanifest'
      - 'requirements.txt'

permissions:
  contents: write

concurrency:
  group: evidencias-ops-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Reconstruir dashboard
        run: |
          python scripts/build_dashboard.py
          python scripts/export_excel.py
          python scripts/export_pdf.py

      - name: Validar proyecto
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: |
          python tests/validate_project.py
          python scripts/audit_project.py

      - name: Publicar únicamente datos generados
        if: github.event_name == 'push'
        shell: bash
        run: |
          if git diff --quiet -- data/dashboard.json exports/Resumen_Evidencias_OPS.xlsx exports/Resumen_Evidencias_OPS.pdf; then
            echo "El Forms no produjo cambios nuevos."
            exit 0
          fi
          git add data/dashboard.json exports/Resumen_Evidencias_OPS.xlsx exports/Resumen_Evidencias_OPS.pdf
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "data: actualizar evidencias OPS"
          git push
